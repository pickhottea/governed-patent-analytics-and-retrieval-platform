# Semantic Governance Policies

These files are the adopted governance policies for the current `patent_led` semantic pipeline.

Source of origin:
- `~/semantic_vector_embedding/policies/semantic/`

Purpose:
- define chunking contract
- define embedding contract
- define dataset selection
- define Google vs EPO lane rules
- define claim presence / claim gate rules

Rule:
- do not delete or rename without updating the active semantic pipeline and documentation
- current project should treat these files as the authoritative local copy for semantic rebuild work
